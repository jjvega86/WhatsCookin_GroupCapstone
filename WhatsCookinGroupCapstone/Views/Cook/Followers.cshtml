@model IEnumerable<WhatsCookinGroupCapstone.Models.Cook>

@{
    ViewData["Title"] = "Followers";
}

<h1>Followers</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.UserName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Bio)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdentityUser)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.UserName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Bio)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdentityUser.Id)
            </td>
            @*<td>
                <a asp-action="Edit" asp-route-id="@item.CookId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CookId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CookId">Delete</a>
            </td>*@
        </tr>
}
    </tbody>
</table>
